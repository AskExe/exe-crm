# Brand-drift allowlist — files allowed to contain the literal "twenty" (case-insensitive)
#
# Rules:
#   - One path per line. Blank lines and lines starting with '#' are comments.
#   - Globs are NOT supported — list each file explicitly so reviewers see what
#     they're approving.
#   - Every entry must be paired with a comment line above it explaining WHY.
#   - The CI check (.github/workflows/ci-brand-drift.yaml) reads this file to
#     decide which files to skip when scanning PR-added lines.

# AGPL §5 attribution — original Twenty copyright must be preserved.
LICENSE

# This document IS the upstream sync runbook — references upstream by name.
UPSTREAM.md

# This allowlist itself documents permitted occurrences and references upstream.
.brand-drift-allowlist.txt

# AGPL §5 attribution preamble — modification notice naming the upstream work.
README.md

# AGPL §5 attribution — list of modifications and upstream source URL.
NOTICE
packages/twenty-server/src/engine/guards/jwt-auth.guard.ts

# --- Unified-permissions auth enforcement (PR #77) — approved 2026-08-05 ---
# Framework-internal refs only in the fork's server tree: twenty-shared /
# twenty-config / twenty-orm import paths, TwentyConfigService identifiers,
# and developer comments explaining upstream Twenty's role/workspace model.
# Verified: no user-facing branding (no UI text, emails, titles, or
# user-visible logs) in any of these files.

# Comments documenting Twenty's one-role-per-userWorkspace constraint.
packages/twenty-server/src/engine/core-modules/auth/constants/exe-managed-roles.constant.ts

# twenty-shared imports + comments on Twenty-native login-token handoff flow.
packages/twenty-server/src/engine/core-modules/auth/controllers/gotrue-auth.controller.ts

# Import from twenty-standard-application module + comments on seeded roles.
packages/twenty-server/src/engine/core-modules/auth/services/__tests__/role-sync.service.spec.ts

# Comments mapping crm/org caps to Twenty role names.
packages/twenty-server/src/engine/core-modules/auth/services/exe-perms.util.ts

# twenty-standard-application import + comments on workspace-scoped roles.
packages/twenty-server/src/engine/core-modules/auth/services/role-sync.service.ts

# twenty-shared/twenty-config/twenty-orm imports + TwentyConfigService usage.
packages/twenty-server/src/engine/core-modules/auth/token/services/access-token.service.ts

# twenty-shared/twenty-config imports + TwentyConfigService usage.
packages/twenty-server/src/engine/core-modules/auth/token/services/renew-token.service.ts

# TwentyConfigService mock in unit test — module name only.
packages/twenty-server/src/engine/core-modules/error-forwarding/__tests__/error-forwarding.service.spec.ts

# TwentyConfigService injection for monitor env config — module name only.
packages/twenty-server/src/engine/core-modules/error-forwarding/error-forwarding.service.ts
# 2026-08-06 (exe/COO): internal Docker build-stage names (AS twenty-app-dev / AS twenty)
# — any FROM-line change (e.g. dependabot base-image bumps) re-adds them in the diff.
# Framework-internal, not user-facing branding.
packages/twenty-docker/twenty/Dockerfile

# --- Supply-chain pin hardening (PR #108) — bug 112eb501 ---
# These four files live in package trees whose UPSTREAM NAMES contain "twenty"
# (create-twenty-app, twenty-apps, twenty-sdk); the literal is unavoidable in
# their paths and in the identifiers they already export. The PR-added lines are
# not new branding — they are supply-chain HARDENING that references upstream in
# order to pin or forbid it. Each entry is narrowly justified below.

# Replaces the mutable tag 'twentycrm/twenty-app-dev:latest' (already present
# before this PR) with an immutable @sha256 digest pin. The upstream namespace
# string is pre-existing and is REDUCED in risk, not introduced.
packages/twenty-sdk/src/cli/utilities/server/docker-container.ts

# Generates the customer's dev-server CI. Names the local `yarn twenty server`
# commands the SDK itself exposes — the fork's own CLI verb, not upstream infra.
packages/twenty-apps/hello-world/.github/workflows/ci.yml

# Scaffolding template + its spec. The spec's added lines are NEGATIVE
# assertions (`expect(content).not.toMatch(/twentyhq\//)`) proving generated
# projects carry no upstream org/registry refs — the guard was matching the very
# strings that prove the fix works.
packages/create-twenty-app/src/utils/test-template.ts
packages/create-twenty-app/src/utils/__tests__/test-template.spec.ts

# --- Non-existent Postgres tag fix (bug a842d0b3) ---
# These three files pin the DB image for the k8s path. The PR-added lines
# REPLACE the tag `:16` -- which the registry 404s, so this deployment could
# never start -- with an immutable @sha256 digest. The `twentycrm/` namespace
# is pre-existing and its risk is REDUCED, not introduced: a digest cannot be
# repointed by a third party the way a tag can. Same justification as the
# twenty-sdk entry above.
packages/twenty-docker/k8s/manifests/deployment-db.yaml
packages/twenty-docker/k8s/terraform/variables.tf
packages/twenty-docker/k8s/terraform/README.md
