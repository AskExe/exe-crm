# DISABLED in Phase 1 (T1-05): Twenty's cross-repo deploy targets
# twentyhq/twenty-infra, which we don't have access to. Self-hosted deploy
# pipeline lands in Phase 3.
#
# Original content preserved in git history (commit before T1-05) for the
# Phase 3 rewrite reference. The stub below makes the workflow valid YAML
# and switches the trigger to manual-only (workflow_dispatch) so tag pushes
# no longer attempt to dispatch to twentyhq/twenty-infra.

name: CD deploy tag (DISABLED)

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Workflow disabled
        run: echo "cd-deploy-tag is disabled — see header comment in this file."
